"use client";

import useLocalStorage from "use-local-storage";
import { NextFont } from "@next/font";

export interface INavbar {
  primaryFont: NextFont;
}

const Favoritos = (props: INavbar) => {
  return(
    <div></div>
  );
};

export default Favoritos